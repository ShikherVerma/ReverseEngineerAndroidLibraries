# ReverseEngineerAndroidLibraries

This project contains Android sample projects used to reverse engineer popular libraries like Butterknife and Retrofit.
Each library has a branch named `lib/<library name>`.
